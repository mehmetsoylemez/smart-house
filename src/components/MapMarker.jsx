import React, { Component } from "react";

export default class MyRestaurantPlace extends Component {
  render() {
    return (
      <i className="material-icons" style={{ fontSize: "50px", color: "red" }}>
        add_location
      </i>
    );
  }
}
